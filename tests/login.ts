import { test, expect } from '@playwright/test';
import { LoginPage } from '../pages/login_page';


test('clean login', async ({page}) => {
    lpage : LoginPage;
    lPage = new LoginPage();

    
    
})